name: qr_scanner_alternative_example
description: Demonstrates how to use the qr_scanner_alternative plugin for Android and iOS.
publish_to: 'none'
version: 1.0.0-nullsafety.0

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1+1
  flutter_plugin_android_lifecycle: ^2.0.0-nullsafety

dev_dependencies:
  flutter_test:
    sdk: flutter

  qr_scanner_alternative:
    path: ../

  pedantic: ^1.10.0-nullsafety.3

flutter:
  uses-material-design: true
